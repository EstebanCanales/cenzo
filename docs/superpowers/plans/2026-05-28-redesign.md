# Censo UI Redesign — Sharp Light — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rediseñar la UI de Censo hacia "Sharp Light": Inter + IBM Plex Mono, tokens minimalistas (radios pequeños, sin sombras, sin grid-pattern), y cuenta de usuario fija al fondo del sidebar.

**Architecture:** Se reemplazan los tokens CSS, se actualiza la tipografía y se refactoriza `AppSidebar` de client → server component (para hacer `auth()` sin pasar props desde `DashboardLayout`). El nav interactivo y el estado mobile se extrae a un nuevo `SidebarClient` client component. La lógica de datos, rutas y auth no cambia.

**Tech Stack:** Next.js 15 (App Router), CSS custom properties, `@fontsource/inter`, `@fontsource/ibm-plex-mono`, TypeScript, Vitest

---

## File Map

| Acción | Archivo | Responsabilidad |
|---|---|---|
| Modify | `apps/web/package.json` | Swap `@fontsource/montserrat` → `@fontsource/inter` + `@fontsource/ibm-plex-mono` |
| Modify | `apps/web/app/layout.tsx` | Cambiar font imports |
| Modify | `apps/web/app/styles/tokens.css` | Todos los tokens: colores, radios, fuentes |
| Modify | `apps/web/app/styles/base.css` | Body background (sin grid), tipografía |
| Modify | `apps/web/app/styles/ui.css` | Radios y colores de primitivos UI |
| Modify | `apps/web/app/styles/dashboard.css` | Sidebar, command surface, metrics, evidence, charts |
| Create | `apps/web/components/dashboard/sidebar-client.tsx` | Client component: nav activo + toggle mobile + user menu |
| Modify | `apps/web/components/dashboard/app-sidebar.tsx` | Convertir a server component: fetch auth, renderiza SidebarClient |
| Modify | `apps/web/app/dashboard/layout.tsx` | Quitar toolbar flotante con UserMenu |
| Modify | `apps/web/app/dashboard/page.tsx` | Cambiar `heading` prop (quitar "Agro Tech Lab") |
| Modify | `apps/web/app/page.tsx` | Landing hero card más limpio |
| Modify | `apps/web/lib/dashboard.test.ts` | Actualizar CSS contract tests a nuevos valores |

---

## Task 1: Fuentes — swap Montserrat → Inter + IBM Plex Mono

**Files:**
- Modify: `apps/web/package.json`
- Modify: `apps/web/app/layout.tsx`

- [ ] **Instalar nuevas fuentes y quitar la vieja**

```bash
cd apps/web
npm install @fontsource/inter @fontsource/ibm-plex-mono
npm uninstall @fontsource/montserrat
```

- [ ] **Actualizar `apps/web/app/layout.tsx`**

Reemplazar:
```tsx
import "@fontsource/montserrat/400.css";
import "@fontsource/montserrat/500.css";
import "@fontsource/montserrat/600.css";
import "@fontsource/montserrat/700.css";
```

Con:
```tsx
import "@fontsource/inter/400.css";
import "@fontsource/inter/500.css";
import "@fontsource/inter/600.css";
import "@fontsource/inter/700.css";
import "@fontsource/inter/800.css";
import "@fontsource/ibm-plex-mono/400.css";
import "@fontsource/ibm-plex-mono/500.css";
```

- [ ] **Commit**

```bash
git add apps/web/package.json apps/web/package-lock.json apps/web/app/layout.tsx
git commit -m "chore: swap Montserrat for Inter + IBM Plex Mono"
```

---

## Task 2: Tokens CSS — colores, radios, sombras, fuentes

**Files:**
- Modify: `apps/web/app/styles/tokens.css`

- [ ] **Reemplazar el contenido completo de `apps/web/app/styles/tokens.css`**

```css
:root {
  --font-sans: "Inter", sans-serif;
  --font-mono: "IBM Plex Mono", monospace;
  --bg: #ffffff;
  --bg-grid: transparent;
  --surface: #ffffff;
  --surface-soft: #fafafa;
  --surface-lab: #f5f5f5;
  --text: #111111;
  --text-strong: #111111;
  --muted: #bbbbbb;
  --line: #f0f0f0;
  --line-strong: #e5e5e5;
  --green: #1a7a4a;
  --green-strong: #1a7a4a;
  --green-soft: #edfaf3;
  --amber: #c98b24;
  --amber-soft: #fef3c7;
  --steel: #5f8192;
  --steel-soft: #eff6ff;
  --graphite: #0f2e1a;
  --danger: #b84a3f;
  --radius: 8px;
  --radius-card: 12px;
  --radius-panel: 12px;
  --radius-shell: 12px;
  --radius-pill: 999px;
  --shadow: none;
  --shadow-soft: 0 2px 8px rgba(0, 0, 0, 0.04);
}
```

- [ ] **Commit**

```bash
git add apps/web/app/styles/tokens.css
git commit -m "design: update CSS tokens to Sharp Light values"
```

---

## Task 3: Base CSS — body y tipografía

**Files:**
- Modify: `apps/web/app/styles/base.css`

- [ ] **Actualizar `apps/web/app/styles/base.css`**

Reemplazar el bloque `body` (líneas 10-20):
```css
body {
  margin: 0;
  overflow-y: auto;
  color: var(--text);
  background: var(--bg);
  font-family: var(--font-sans);
}
```

Actualizar `h1`:
```css
h1 {
  margin-bottom: 8px;
  font-size: 32px;
  line-height: 1.05;
  font-weight: 800;
}
```

Añadir después de `h3 { ... }`:
```css
h1, h2, h3 {
  letter-spacing: -0.02em;
}
```

El resto del archivo (`a`, `button`, `input`, `img`, `.page-shell`, `.hero-card`, `.eyebrow`, `.lede`, `.cta-stack`, media queries) no cambia — los tokens se encargan de los colores y radios.

- [ ] **Verificar que no queda ninguna referencia a `--bg-grid` ni al `linear-gradient` de fondo**

```bash
grep -n "bg-grid\|linear-gradient.*bg-grid\|background-size: 32px" apps/web/app/styles/base.css
```

Esperado: sin resultados.

- [ ] **Commit**

```bash
git add apps/web/app/styles/base.css
git commit -m "design: simplify body background, tighten letter-spacing"
```

---

## Task 4: UI CSS — primitivos (botones, badges, cards)

**Files:**
- Modify: `apps/web/app/styles/ui.css`

Los tokens ya actualizaron los colores y radios. Los cambios específicos en este archivo son:

- [ ] **Cambiar `.ui-button--default` box-shadow**

```css
.ui-button--default {
  color: white;
  background: var(--green-strong);
  box-shadow: none;
}
```

- [ ] **Cambiar `.ui-card` box-shadow**

```css
.ui-card {
  border: 1px solid var(--line);
  border-radius: var(--radius-card);
  background: var(--surface);
  box-shadow: none;
}
```

- [ ] **Cambiar `.ui-sheet__content` box-shadow**

```css
.ui-sheet__content {
  position: fixed;
  top: 0;
  right: 0;
  z-index: 51;
  width: min(100%, 440px);
  height: 100vh;
  overflow-y: auto;
  border-left: 1px solid var(--line);
  background: white;
  box-shadow: none;
  padding: 22px;
}
```

- [ ] **Cambiar `.ui-dropdown__content` box-shadow**

```css
.ui-dropdown__content {
  z-index: 70;
  min-width: 220px;
  border: 1px solid var(--line-strong);
  border-radius: 12px;
  background: white;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
  padding: 6px;
}
```

- [ ] **Commit**

```bash
git add apps/web/app/styles/ui.css
git commit -m "design: remove heavy shadows from UI primitives"
```

---

## Task 5: Dashboard CSS — sidebar

**Files:**
- Modify: `apps/web/app/styles/dashboard.css`

- [ ] **Reemplazar estilos de `.app-frame` y `.app-frame__toolbar`**

Buscar y reemplazar el bloque existente (`.app-frame` + `.app-frame__toolbar`):
```css
.app-frame {
  min-height: 100dvh;
  padding: 0;
}
```

Eliminar completamente `.app-frame__toolbar` (ya no existe en el DOM).

- [ ] **Reemplazar estilos del sidebar**

Buscar el bloque `.app-sidebar { ... }` hasta el cierre del bloque `.app-sidebar__story { ... }` y reemplazarlo:

```css
.app-sidebar {
  position: sticky;
  top: 14px;
  display: flex;
  min-height: calc(100dvh - 28px);
  flex-direction: column;
  border: 1px solid var(--line);
  border-radius: var(--radius-card);
  background: var(--surface);
  padding: 16px 12px;
}

.app-sidebar__brand {
  display: flex;
  align-items: center;
  gap: 9px;
  padding-bottom: 16px;
  border-bottom: 1px solid var(--line);
  margin-bottom: 14px;
}

.app-sidebar__brand p {
  margin-bottom: 1px;
  font-size: 13px;
  font-weight: 700;
  letter-spacing: -0.02em;
}

.app-sidebar__brand span {
  color: var(--muted);
  font-size: 10px;
}

.app-sidebar__logo {
  display: grid;
  width: 28px;
  height: 28px;
  place-items: center;
  border-radius: 8px;
  color: var(--green);
  background: var(--graphite);
}

.app-sidebar__nav {
  display: flex;
  flex-direction: column;
  gap: 1px;
}

.app-sidebar__link {
  position: relative;
  display: flex;
  align-items: center;
  gap: 9px;
  overflow: hidden;
  border-radius: 8px;
  color: var(--muted);
  font-size: 12px;
  font-weight: 500;
  padding: 8px 10px;
}

.app-sidebar__link--active {
  color: var(--text-strong);
  background: var(--surface-soft);
  font-weight: 600;
}

.app-sidebar__link-indicator {
  width: 5px;
  height: 5px;
  border-radius: 999px;
  background: var(--green);
  margin-left: auto;
  flex-shrink: 0;
}

.app-sidebar__bottom {
  margin-top: auto;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.app-sidebar__story {
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--surface-soft);
  padding: 11px 12px;
}

.app-sidebar__story span {
  display: block;
  margin-bottom: 4px;
  color: var(--muted);
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.app-sidebar__story p {
  color: var(--muted);
  font-size: 10px;
  line-height: 1.5;
}

.sidebar-account {
  display: flex;
  align-items: center;
  gap: 9px;
  padding: 9px 10px;
  border-top: 1px solid var(--line);
  border-radius: 8px;
  cursor: pointer;
}

.sidebar-account:hover {
  background: var(--surface-soft);
}
```

- [ ] **Commit**

```bash
git add apps/web/app/styles/dashboard.css
git commit -m "design: restyle sidebar — small radius, account at bottom"
```

---

## Task 6: Dashboard CSS — command surface, métricas y grids

**Files:**
- Modify: `apps/web/app/styles/dashboard.css`

- [ ] **Actualizar `.command-surface`**

```css
.command-surface {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 220px;
  gap: 0;
  align-items: stretch;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--radius-card);
  background: var(--surface-soft);
  box-shadow: none;
}

.command-surface__greeting {
  background: var(--surface-soft);
  padding: 16px 20px;
}

.command-surface__greeting h2 {
  margin-bottom: 6px;
  font-size: 17px;
  letter-spacing: -0.02em;
}

.command-surface__greeting p:last-child {
  max-width: 720px;
  margin-bottom: 0;
  color: var(--muted);
  font-size: 11px;
}

.command-surface__signal {
  display: grid;
  align-content: center;
  gap: 4px;
  border-left: 1px solid var(--line);
  background: var(--graphite);
  color: white;
  padding: 16px;
}

.command-surface__signal span,
.command-surface__signal p {
  color: rgba(255, 255, 255, 0.4);
  font-size: 9px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

.command-surface__signal strong {
  font-size: 24px;
  font-family: var(--font-mono);
  color: var(--green);
  text-shadow: 0 0 20px rgba(26, 122, 74, 0.3);
}
```

- [ ] **Actualizar `.metrics-strip` y `.metric-card`**

```css
.metrics-strip {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 0;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--radius-card);
  background: var(--surface);
  box-shadow: none;
}

.metric-card {
  min-height: 96px;
  border: 0;
  border-right: 1px solid var(--line);
  border-radius: 0;
  box-shadow: none;
}

.metric-card__content strong {
  font-size: 24px;
  line-height: 1;
  font-family: var(--font-mono);
  letter-spacing: -0.02em;
}
```

- [ ] **Actualizar `.evidence-panel` y `.lab-chart`**

```css
.evidence-panel {
  display: grid;
  grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
  min-height: 390px;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--radius-card);
  background: var(--surface);
  box-shadow: none;
}

.lab-chart {
  border: 1px solid var(--line);
  border-radius: var(--radius-card);
  background: var(--surface);
  box-shadow: none;
  padding: 14px 16px;
}
```

- [ ] **Quitar el bloque de media-query `app-frame__toolbar` en la breakpoint 1120px**

Buscar y eliminar este bloque dentro del `@media (max-width: 1120px)`:
```css
.app-frame__toolbar {
  position: static;
  padding: 14px 14px 0;
  margin-bottom: 10px;
}
```

- [ ] **Commit**

```bash
git add apps/web/app/styles/dashboard.css
git commit -m "design: restyle command surface, metrics strip, evidence panel"
```

---

## Task 7: Refactorizar AppSidebar — server + SidebarClient

**Files:**
- Create: `apps/web/components/dashboard/sidebar-client.tsx`
- Modify: `apps/web/components/dashboard/app-sidebar.tsx`

El objetivo es convertir `AppSidebar` en server component para que pueda llamar `auth()` y mostrar la cuenta de usuario al fondo del sidebar. El estado interactivo (nav activo, toggle mobile) se mueve a `SidebarClient`.

- [ ] **Crear `apps/web/components/dashboard/sidebar-client.tsx`**

```tsx
"use client";

import { BarChart3, Blocks, ChevronDown, Home, Leaf, LogOut, Menu } from "lucide-react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { useState } from "react";

import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { cn } from "@/lib/utils";

const navItems = [
  { href: "/dashboard", label: "Overview", icon: Home },
  { href: "/dashboard/products", label: "Products", icon: Blocks },
  { href: "/dashboard/graphs", label: "Graphs", icon: BarChart3 },
];

type SidebarClientProps = {
  name: string;
  email: string;
  initials: string;
  onSignOut: () => Promise<void>;
};

export function SidebarClient({ name, email, initials, onSignOut }: SidebarClientProps) {
  const pathname = usePathname();
  const [open, setOpen] = useState(false);

  return (
    <>
      <button
        className="mobile-sidebar-toggle"
        onClick={() => setOpen((v) => !v)}
        type="button"
      >
        <Menu size={18} />
        Menu
      </button>

      <aside className={cn("app-sidebar", open && "app-sidebar--open")}>
        <div className="app-sidebar__brand">
          <div className="app-sidebar__logo">
            <Leaf size={16} />
          </div>
          <div>
            <p>Censo</p>
            <span>agri trust layer</span>
          </div>
        </div>

        <nav className="app-sidebar__nav">
          {navItems.map((item) => {
            const active =
              item.href === "/dashboard"
                ? pathname === item.href
                : pathname.startsWith(item.href);
            const Icon = item.icon;

            return (
              <Link
                key={item.href}
                className={cn("app-sidebar__link", active && "app-sidebar__link--active")}
                href={item.href}
                onClick={() => setOpen(false)}
              >
                <Icon size={14} />
                <span>{item.label}</span>
                {active ? <span className="app-sidebar__link-indicator" /> : null}
              </Link>
            );
          })}
        </nav>

        <div className="app-sidebar__bottom">
          <div className="app-sidebar__story">
            <span>Stellar</span>
            <p>Verifica evidencia y reduce manipulación en la cadena agrícola.</p>
          </div>

          <DropdownMenu>
            <DropdownMenuTrigger asChild>
              <button className="sidebar-account" type="button">
                <Avatar>
                  <AvatarFallback>{initials}</AvatarFallback>
                </Avatar>
                <div style={{ flex: 1, minWidth: 0, textAlign: "left" }}>
                  <strong style={{ display: "block", fontSize: "11px", fontWeight: 700, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                    {name}
                  </strong>
                  <span style={{ display: "block", fontSize: "10px", color: "var(--muted)", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                    {email}
                  </span>
                </div>
                <ChevronDown size={14} style={{ color: "var(--muted)", flexShrink: 0 }} />
              </button>
            </DropdownMenuTrigger>
            <DropdownMenuContent align="end" side="top">
              <form action={onSignOut}>
                <DropdownMenuItem asChild>
                  <button className="dropdown-action-button" type="submit">
                    <LogOut size={14} />
                    Cerrar sesión
                  </button>
                </DropdownMenuItem>
              </form>
            </DropdownMenuContent>
          </DropdownMenu>
        </div>
      </aside>
    </>
  );
}
```

- [ ] **Reemplazar el contenido de `apps/web/components/dashboard/app-sidebar.tsx`**

```tsx
import { auth, signOut } from "@/auth";

import { SidebarClient } from "./sidebar-client";

export async function AppSidebar() {
  const session = await auth();
  const name = session?.user?.name ?? "Agricultural operator";
  const email = session?.user?.email ?? "operator@censo.local";
  const initials = name
    .split(" ")
    .map((part) => part[0])
    .join("")
    .slice(0, 2)
    .toUpperCase();

  return (
    <SidebarClient
      email={email}
      initials={initials}
      name={name}
      onSignOut={async () => {
        "use server";
        await signOut({ redirectTo: "/" });
      }}
    />
  );
}
```

- [ ] **Commit**

```bash
git add apps/web/components/dashboard/sidebar-client.tsx apps/web/components/dashboard/app-sidebar.tsx
git commit -m "feat: move user account to sidebar bottom, split sidebar into server/client"
```

---

## Task 8: DashboardLayout — quitar toolbar flotante

**Files:**
- Modify: `apps/web/app/dashboard/layout.tsx`

La cuenta de usuario ya vive en el sidebar. El `UserMenu` en el toolbar ya no se necesita.

- [ ] **Reemplazar el contenido de `apps/web/app/dashboard/layout.tsx`**

```tsx
import { redirect } from "next/navigation";
import { ReactNode } from "react";

import { auth } from "@/auth";

export default async function DashboardLayout({ children }: { children: ReactNode }) {
  const session = await auth();

  if (!session?.user) {
    redirect("/");
  }

  return <div className="app-frame">{children}</div>;
}
```

- [ ] **Eliminar `apps/web/components/dashboard/user-menu.tsx`** (ya no es importado por nadie)

```bash
rm apps/web/components/dashboard/user-menu.tsx
```

- [ ] **Commit**

```bash
git add apps/web/app/dashboard/layout.tsx
git rm apps/web/components/dashboard/user-menu.tsx
git commit -m "refactor: remove floating toolbar and dead UserMenu component"
```

---

## Task 9: Dashboard page — quitar "Agro Tech Lab"

**Files:**
- Modify: `apps/web/app/dashboard/page.tsx`

- [ ] **Cambiar el `heading` prop de `LabShell`**

Buscar:
```tsx
heading="Agro Tech Lab"
```

Reemplazar:
```tsx
heading={`${greeting}, ${user}.`}
```

El componente `LabShell` ya recibe `heading` y lo renderiza como `<h1>`. El saludo ya está disponible en `greeting` y `user` en el mismo scope.

- [ ] **Commit**

```bash
git add apps/web/app/dashboard/page.tsx
git commit -m "design: replace static 'Agro Tech Lab' heading with user greeting"
```

---

## Task 10: Landing page — hero card limpio

**Files:**
- Modify: `apps/web/app/page.tsx`

Los tokens ya cambian el `background`, `border-radius` y colores. El único cambio estructural es el botón de Google.

- [ ] **Verificar que el botón de Google usa `Button size="lg"`**

En `apps/web/app/page.tsx` el botón ya existe. No necesita cambios estructurales — los tokens se encargan del estilo.

- [ ] **Verificar visualmente la landing en `http://localhost:3000`**

Debe mostrar:
- Fondo `#fafafa` (sin grid-pattern)
- Hero card con `border-radius: 12px`, sin sombra pesada
- Botón oscuro (hereda del token `--green-strong` que ahora es `#1a7a4a`)

- [ ] **Commit si hay cambios menores de clase**

```bash
git add apps/web/app/page.tsx
git commit -m "design: landing hero card inherits new tokens"
```

---

## Task 11: Actualizar CSS contract tests

**Files:**
- Modify: `apps/web/lib/dashboard.test.ts`

Los tests de "Soft Connected OS style contract" verifican los valores CSS anteriores. Ahora deben verificar los nuevos valores.

- [ ] **Actualizar el test `"uses rounded connected surfaces..."`**

Buscar:
```ts
it("uses rounded connected surfaces instead of small card radii", () => {
  expect(tokensCss).toContain("--radius-shell: 34px");
  expect(tokensCss).toContain("--radius-panel: 30px");
  expect(tokensCss).toContain("--radius-pill: 999px");
});
```

Reemplazar con:
```ts
it("uses sharp card radii (Sharp Light design)", () => {
  expect(tokensCss).toContain("--radius-shell: 12px");
  expect(tokensCss).toContain("--radius-panel: 12px");
  expect(tokensCss).toContain("--radius-pill: 999px");
  expect(tokensCss).toContain("--font-sans: \"Inter\"");
  expect(tokensCss).toContain("--font-mono: \"IBM Plex Mono\"");
});
```

- [ ] **Correr los tests para verificar que pasan**

```bash
cd apps/web
npm test
```

Esperado: todos los tests pasan incluyendo el renombrado.

- [ ] **Commit**

```bash
git add apps/web/lib/dashboard.test.ts
git commit -m "test: update CSS contract tests to Sharp Light values"
```

---

## Task 12: Verificación visual final

- [ ] **Correr el frontend**

```bash
cd apps/web
npm run dev
```

- [ ] **Verificar landing (`http://localhost:3000`)**
  - Fondo blanco/gris claro, sin grid
  - Hero card con bordes finos y radio 12px
  - Botón "Continuar con Google" oscuro

- [ ] **Iniciar sesión y verificar dashboard (`http://localhost:3000/dashboard`)**
  - Sidebar: marca arriba, nav en medio, Stellar + cuenta al fondo
  - Header: muestra el saludo (sin "Agro Tech Lab")
  - Métricas: valores en IBM Plex Mono, sin sombras
  - Command surface: fondo `#fafafa`, panel signal oscuro

- [ ] **Verificar `/dashboard/products` y `/dashboard/graphs`**
  - Mismo sidebar con cuenta al fondo
  - Layouts sin cambios funcionales

- [ ] **Correr tests**

```bash
cd apps/web && npm test
```

Esperado: todos los tests pasan.

- [ ] **Commit final si hubo correcciones menores**

```bash
git add -p
git commit -m "design: sharp light redesign — final polish"
```
